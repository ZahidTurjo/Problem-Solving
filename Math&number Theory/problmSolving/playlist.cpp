#include<bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin>>n;
    vector<int>f(n,0);
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        if(f[x]==0){
            
        }
    }
  cout<<mx<<'\n';
    return 0;
}